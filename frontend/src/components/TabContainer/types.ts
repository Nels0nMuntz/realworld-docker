export interface StickyTabContainerProps extends React.PropsWithChildren {
  isScrollbarVisible: boolean;
}
export interface ScrollableTabContainerProps extends React.PropsWithChildren {
  isScrollbarVisible: boolean;
  isShadowVisible: boolean;
}
